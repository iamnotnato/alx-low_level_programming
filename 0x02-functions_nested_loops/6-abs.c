#include "main.h"

/**
 * _abs - Entry Point
 * @n : parameter
 * Description: a function that
 * computes the absolute value of an integer.
 * Return: Always 0.
 */

int _abs(int n)
{
	if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (n);
	}
}
