#include "main.h"
#include <stdio.h>
/**
* add - adds two integers and returns the result.
* starting with 0.
* @n: The number in question.
* @m: The number in question.
*
* Return: void
*/

int add(int n, int m)
{
int sum;

sum = n + m;
return (sum);
}
