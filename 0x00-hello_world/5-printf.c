#include <stdio.h>

/**
 * main - Entry Point
 * Description: a program that prints
 * exactly with proper grammar,
 * but the outcome is a piece of art,
 * followed by a new line
 * Return: Always 0.
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
